server:
  port: 8085
  servlet:
    context-path: /weather

jwt:
  signerKey: ${JWT_SIGNER_KEY}

app:
  service:
    url:
      weather-client: ${WEATHER_CLIENT}

open-weather-map:
  api-key: ${WEATHER_API_KEY}
  lang: ${WEATHER_LANG}
  units: ${WEATHER_UNITS}