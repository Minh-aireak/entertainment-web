server:
  port: 9000

spring:
  application:
    name: debezium-manager
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

app:
  services:
    debezium:
      connect-url: ${DEBEZIUM_CONNECT_URL:http://localhost:8100}
